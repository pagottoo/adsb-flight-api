# Copie para .env e preencha. O .env NÃO é commitado (ver .gitignore).

# Token de longa duração do Home Assistant (Perfil -> Long-Lived Access Tokens).
# Deixe vazio para rodar só o servidor HTTP, sem a integração de voz.
HA_TOKEN=
